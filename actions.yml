# Define your actions below.
# Actions should have no spaces in the name.


actions:
  - name: Job finishes successfully
    enabled: true

    triggered_by:
      - render_job_completed

    steps:
      - telegram:
          chat_id: "telegram chat ID"
          message: "message #1"

      - telegram:
          chat_id: "telegram chat ID secondperson"
          message: "message #2!"

      - email:
          subject: "subject"
          to: "to"
          message: "email body"


  - name: Dummy 2nd Action that fails
    enabled: false

    triggered_by:
      - render_job_hahaha

    steps:
      - other_custom_step:
          param: value
